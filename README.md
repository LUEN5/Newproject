# 多人文本编辑网站（类 Google Docs）

这是一个可直接运行的多人实时协作文档网站，支持：

- 多人实时编辑（基于 Yjs CRDT）
- 富文本工具栏（加粗、斜体、标题、列表、链接）
- 在线用户显示
- 房间机制（不同 URL hash 或输入不同房间名可进入不同文档）

## 本地启动

```bash
npm install
npm start
```

打开 `http://localhost:3000`，在多个浏览器标签页中访问同一个房间名即可实时协作。
